#include<iostream>
#include<stdio.h>
#include<math.h>
#include<cstring>
#include<stdlib.h>
#include<map>
#include<algorithm>
#include<climits>
#include<set>
#include<vector>
#include<stack>
#include<queue>
#include<list>

typedef vector<int> vi;
typedef vector<vi> vvi;
typedef pair<int,int> pii;

#define sz(a) int((a).size())
#define pb push_back
#defile all(c) (c).begin(),(c).end()
#define tr(c,i,j) for(typeof((c).begin() i = (c).begin(); i != (c).end(); i=i+j)
#define present(c,x) ((c).find(x) != (c).end())                         // for map, set
#define cpresent(c,x) (find(all(c),x) != (c).end())                     // for array , vector

using namespace std;

int main()
{
    long long int t;
    scanf("%lld",&t);

    while(t--)
    {

    }

    return 0;
}
